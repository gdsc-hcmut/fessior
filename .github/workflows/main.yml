name: Deploy

on:
  push:
    branches:
      - chore/setup-test

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy
        run: |
          echo "Add deploy script later..."
